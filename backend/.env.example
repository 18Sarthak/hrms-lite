# Database Configuration
# For local development (SQLite)
# DATABASE_URL=sqlite:///./hrms_lite.db

# For production (PostgreSQL on Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# CORS Configuration
# For production, specify exact frontend URLs (comma-separated)
# Example: ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://www.yourdomain.com
# For development, leave as "*" or omit to allow all origins
# ALLOWED_ORIGINS=*
